{
  "routes": [
    {
      "method": "GET",
      "path": "/available-times",
      "handler": "available-time.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/available-times/count",
      "handler": "available-time.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/available-times/:id",
      "handler": "available-time.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/available-times",
      "handler": "available-time.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/available-times/:id",
      "handler": "available-time.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/available-times/:id",
      "handler": "available-time.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
